# Code coverage (gcov). Set to 1 to build this project with coverage.
# Off by default. Per-build alternative: the --coverage flag on
# change-compiler.sh / build-all.sh / update / update-all / setup.
0
